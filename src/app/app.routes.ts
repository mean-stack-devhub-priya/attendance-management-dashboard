import { Routes } from '@angular/router';
import { ShiftManagement } from './components/shift-management/shift-management';

export const routes: Routes = [
    {path:'',component:ShiftManagement}
];
